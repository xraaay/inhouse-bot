// const { GuildChannelManager } = require('discord.js')
// const guildChannelManager = new GuildChannelManager();

module.exports = {
    name: 'createChannel',
    description: 'Creates a voice channel',
    args: false,
    execute(message, args, client) {
        // client.guild.channels.create("test")
    },
};